# local-edge examples

This directory holds example workloads for the optional local-edge environment.

- `personal-apps/`: loopback-bound host or Docker examples fronted directly by Caddy
- `kind-workload/`: Kubernetes example workload fronted through the kind ingress entrypoint

Run `./scripts/bootstrap-local-edge.sh setup` before using any example command.
