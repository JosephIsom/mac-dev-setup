# shellcheck shell=bash
# shellcheck disable=SC1091
# git-fzf / forgit integration
if [[ -r "$HOME/.zsh/vendor/forgit/forgit.plugin.zsh" ]]; then
  source "$HOME/.zsh/vendor/forgit/forgit.plugin.zsh"
fi
