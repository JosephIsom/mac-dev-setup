# User-local tmux overrides (mac-dev-setup)
# Useful built-ins:
# - prefix + I: install/update TPM plugins after config changes
# - prefix + Ctrl-s: save tmux session with tmux-resurrect
# - prefix + Ctrl-r: restore tmux session with tmux-resurrect
