# Local Ghostty overrides (mac-dev-setup)
# Add machine-specific or personal overrides here.
#
# Available managed themes are installed into ~/.config/ghostty/themes/.
# Example:
# theme = apple-graphite-expanded-light
